use "../api/combinators.npk".*;
use "../api/builder.npk".*;

pub func:npk_example_lisp_parser = wild GrammarCtx->() {
    int64:g_ptr = raw npk_grammar_create();
    wild GrammarCtx->:g = @cast_unchecked<GrammarCtx->>(g_ptr);
    pass(g);
};
